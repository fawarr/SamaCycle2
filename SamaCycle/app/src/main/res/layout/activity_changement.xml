<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_changement"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/pink"
    tools:context="projetmobile.sn.Changement">

    <Button
        android:text="continuer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="31dp"
        android:id="@+id/Continuer"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="127dp"
        android:onClick="Continuer"/>

    <Button
        android:text="s'inscrire"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="23dp"
        android:id="@+id/inscription"
        android:layout_alignBaseline="@+id/Continuer"
        android:layout_alignBottom="@+id/Continuer"
        android:layout_alignParentEnd="true"
        android:onClick="Inscrit" />

    <TextView
        android:text="Bienvenue:Voulez vous continuez ou changer vos informations personnelles?"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="28dp"
        android:id="@+id/bien"
        android:layout_above="@+id/Continuer"
        android:layout_marginBottom="65dp" />
</RelativeLayout>

